This directory is kept as a Dokploy compatibility stub.

The production app has moved to the root Next.js + Payload application.
Dokploy still has an old `backend` service pointed at `admin-zone/apps/backend`,
so this stub prevents automatic deploys from failing hard while the Dokploy
configuration is being migrated.
